## 1. 基礎設施與極簡風格配置 (Foundation)

- [x] 1.1 設定 `package.json` 並安裝核心套件 (`express`, `sqlite3`, `cors`)。
- [x] 1.2 建立 `public` 目錄與 HTML 骨架：採用 Tab 切換模式區分「趨勢觀察」與「資料管理」。
- [x] 1.3 編寫 `style.css`：實作白底黑黃橘配色，確保「資料管理」區塊同樣具備圖表展示空間且易於操作。
- [x] 1.4 設定 `.gitignore`（排除 `node_modules/` 與 SQLite 檔案），並在 `package.json` 加入 `"start": "node index.js"` 指令。

## 2. 後端與持久化資料庫 (Backend & DB)

- [x] 2.1 實作 `src/db.js`：建立 `prices` 表（含唯一約束）與 `inflation_rates` 表。
- [x] 2.2 實作 `index.js`：開發 REST API 端點，包含新增、查詢與獲獲儀表板總覽。
- [x] 2.3 匯入初始數據與通膨參考數據：設定區間為 2016/05 至 2026/04。

## 3. 圖表視覺化與互動開發 (Visualization & JS Logic)

- [x] 3.1 實作圖表渲染模組：使用 Chart.js 顯示 2016/05 - 2026/04 趨勢。
- [x] 3.2 實作通膨平減計算：前端計算實質價格。
- [x] 3.3 實作高亮機制：搜尋命中時於圖表標記「橘黃色發光效果」。
- [x] 3.4 確保前端使用原生 `fetch` API 與 Express 後端進行資料溝通。

## 4. 介面與功能整合 (Integration & UX Flow)

- [x] 4.1 實作觀察區搜尋：搜尋成功後，表格「僅顯示單筆」，圖表標記同步。
- [x] 4.2 實作管理區：獨立分頁，**包含圖表連動**，提供扁平表格（年/月/價格）與多維篩選（年/月/價格區間）。
- [x] 4.3 實作資料連動：管理區刪改或篩選後，自動更新觀察區與管理區自身的趨勢圖。
- [x] 4.4 實作「搜尋無資料」引導：自動將搜尋參數帶入新增表單。
- [x] 4.5 最終 UI 乾淨度調整：使用 Flexbox/Grid 確保充足留白，移除非必要的 UI 線條。
